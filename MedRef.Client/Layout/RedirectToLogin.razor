@inject NavigationManager Navigation
@* This component redirects users to the login page if they are not authenticated *@
@code {
    protected override void OnInitialized()
    {
        // Redirects unauthorized or logged-out users back to the clean home page card layout!
        Navigation.NavigateTo("/", forceLoad: false);
    }
}